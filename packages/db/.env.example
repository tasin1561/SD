# Local Docker Postgres (see docker/docker-compose.yml at repo root).
# User/pass/db are all `skydrop`. Schema is `public`.
DATABASE_URL="postgresql://skydrop:skydrop@127.0.0.1:5432/skydrop?schema=public"
