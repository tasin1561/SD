import type { ReactElement } from 'react';
import { RolesIndex } from './_components/roles-index';

export const metadata = { title: 'Roles · Skydrop' };

export default function SellerRolesPage(): ReactElement {
  return <RolesIndex />;
}
