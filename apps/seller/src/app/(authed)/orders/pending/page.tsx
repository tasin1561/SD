import type { ReactElement } from 'react';
import { PendingOrdersIndex } from './_components/pending-orders-index';

export default function PendingOrdersPage(): ReactElement {
  return <PendingOrdersIndex />;
}
