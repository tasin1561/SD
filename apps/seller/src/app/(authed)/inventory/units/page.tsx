import type { ReactElement } from 'react';
import { UnitDiscrepanciesIndex } from './_components/unit-discrepancies-index';

export default function UnitsPage(): ReactElement {
  return <UnitDiscrepanciesIndex />;
}
