# MOTION-COVERAGE — apps/track

Phase 2 (2026-09-23). Same guardrails as the consoles: nothing delays a
navigation, reduced motion (OS) lands every pattern in its end state.

- **/**: the corridor map (after first paint, `emphasis={2}`) with its
  flights; the lookup card rises (movement only — the title is the LCP);
  u33 AWB field; label-into-parcel on "Track" (hover/press only, ≤ 350 ms —
  the push is never delayed); u14 theme switch; language pill.
- **/[awb]**: the u17 Timeline IS the page — the connector fills once, the
  latest scan pulses as the current step (paused off-screen), milestones
  still to come follow it, "On the way" progress bar and expected date
  while moving; a journey of more than 8 scans folds the older ones behind
  "Show n earlier scans" (the button stays in place, focus kept); status
  chip with icon + word.
- **/[awb] not found / unavailable**: u27 empty state (the parcel under a
  sweeping magnifier, bob paused off-screen) with one next step.
