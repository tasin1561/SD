# Motion coverage — seller reseller stores

- **/reseller-stores** — Button (u28 sweep) on "Open a reseller store" in the header and in the empty state; KpiCard glow on four cards, all plain counts, so each rolls up once on first load (odometer); skeleton cards and skeleton rows while the list loads; stores waiting for approval and the rest in two section cards, each a data table (mobile card layout below `md`), status as StatusChip (icon + word); hover-only rows, no per-row entrance; "Reports" link arrow steps right on hover/focus; u27 empty state (bobbing parcel) with the primary action when there are no stores; "Open a reseller store" Dialog (scale + fade, bottom sheet under 640px) with u33 floating-label fields (the two 80-character names show their counter), u05 native Select and a rolling-label AsyncButton "Open the store" (Opening…) wired to the create request; success toast (u32).
- **/reseller-stores/[storeId]** — back link, PageHeader breadcrumbs, facts and the store's StatusChip; KpiCard glow on four cards (only Team is a plain count and rolls up; status, wallet manager and date are words); liquid-bead Tabs travelling between Overview / Catalogue & stock / Terms / What they can do, the panel rising in on change (only the chosen panel is mounted, as before); skeletons while the store loads.
  - Overview — decision card (Approve / Reject) and lifecycle card (Pause / Resume / Close for good) with icon chips; Approve, Reject, Close and Invite are Dialogs with a rolling-label AsyncButton each (Approving… / Rejecting… / Closing… / Sending…) wired to the real mutation; Pause, **Resume (new)**, Change wallet manager and Withdraw invitation are ConfirmDialogs (busy confirm, locked while the request runs); details as a fact list; team and history as data tables (hover-only rows); toasts for every outcome.
  - Store wallet — KpiCard glow on three cards (Balance and May-go-below-zero keep their `<Money>` as `figure`, no count-up; Waiting on Skydrop rolls up); skeleton cards / rows while loading; ledger rows carry a direction arrow chip; "Show older" is a rolling-label AsyncButton (Loading… → Loaded) wired to the next page; Top up and Record a payout are Dialogs whose button now opens a **ConfirmDialog (new)** restating the store, the amount and the consequence before the same request fires; the negative-limit Save opens its ConfirmDialog as before; u27 empty state for no movements.
  - Catalogue & stock — table toolbar (search box + u08 Switch "Only products sold here"); "Sold here" as a StatusChip; GlossaryTerm tooltip cards (u35) on "Transfer price" and "Set aside", and in the edit dialog on "Set aside" / "hidden share"; edit Dialog with two u08 Switches, u33 fields, u05 Select, and a rolling-label "Save" (Saving…) that now opens a **ConfirmDialog (new)** restating the store, product, SKU and the new terms; Remove picture ConfirmDialog; skeleton rows while loading; u27 empty states.
  - Terms — versioned card (section heading with a signature icon, credit timing as check-chip rows, critical callout when a revision is needed); the worked example is a data table; publish form in u33 fields, u05 Selects in two tinted fieldsets; the live example line is a polite status region; "Publish version N" opens the existing ConfirmDialog (now busy while publishing); history table with a version pill; u27 empty state before the first version.
  - What they can do — the permission matrix is a data table: the first question "Can the Reseller store do this?" is a u08 Switch with Yes / No inside the track (thumb springs), the second "How?" a u05 Select shown only on Yes; unsaved-changes dot; Save now opens a **ConfirmDialog (new)** restating the store and every changed task (before → after) before the same request fires.
- **/reseller-stores/price-list** — back link; KpiCard glow on three cards (two plain counts roll up, the unit total keeps its `<Num>`); table toolbar search with a Reset action; GlossaryTerm tooltip card on "Transfer price"; hover-only rows; edit Dialog with the u33 price fields and a rolling-label "Save price" (Saving…); Remove price ConfirmDialog; skeletons while loading; u27 empty state (search variant with a Reset action); toasts.
- **/reseller-stores/reports** — KpiCard glow on four cards (counts keep their `<Num>`, margin keeps its `<Money>`, so none re-rolls when the window changes; margin coverage in the card foot); DateField (u33 with calendar chip) for the window; skeletons while loading; three data tables with StatusChips, hover-only rows; auto-pause Dialog with a u08 Switch, u33 fields and a rolling-label AsyncButton "Save" (Saving… → Saved, error shown on the button) wired to the real request; toasts; u27 empty states.
- **/reseller-stores/requests** ("Waiting on you") — KpiCard glow on three cards, plain counts rolling up once; skeleton cards and rows while loading; **positive** u27 empty state (check art) when nothing is waiting, and positive empty states per queue; three data tables with StatusChips and the address comparison (old value struck through → new value); **Approve now opens a ConfirmDialog in all three queues (new)**, restating the store, the order number (identifier face) and what approving does (for an address change, the full list of changes); Reject opens the reason Dialog (critical tone) with a u33 textarea showing its existing 2,000-character counter and a rolling-label "Turn it down" (Sending…); the row's verdict stays under its buttons with an icon; toasts.
- **/reseller-stores/stock-forecast** — KpiCard glow on three cards (two plain counts roll up; units keep `<Num>`; "—" when the forecast failed); skeletons while loading; data table with product thumbnails and SKU in the identifier face; "Reorder" StatusChip rings once on mount (pulse); u27 empty state with a link back to the stores.

Reduced motion: every pattern above is handled by the global collapse in `brand/app.css`; end states (switch positions, the bead on the chosen tab, final figures, confirm dialogs) are correct with no motion. Nothing delays navigation: every link is a plain link with hover feedback only.
