# Motion coverage — seller wallet and inbound freight

- **/wallet** — Button (u28 sweep) on "Top-up wallet" and "Request a withdrawal"; KpiCard glow in the balance's tone on both balance cards (money figures handed over as `figure`, so no count-up by design); Tabs liquid bead travelling between Ledger / Top-ups / Withdrawal requests, with the panel rising in on change; rolling-label AsyncButton on "Export CSV" (Loading all… → Downloaded), "Load more" (Loading… → Loaded) and each top-up's "View receipt" (Opening… → Opened); skeleton shimmer while the balance, ledger, top-ups and withdrawals load; u27 empty state (bobbing parcel) for an empty ledger, no top-ups (action: Top-up wallet) and no withdrawals (action: Request a withdrawal); ledger rows carry a direction arrow chip (no per-row animation); top-up wizard in a Dialog (scale + fade, bottom sheet under 640px) driving the u34 Stepper (connector fills, done steps turn to checks, current step glows, content rises in) — bank cards lift on hover/focus, the proof upload is the u15 DropZone (lifts and tints on drag), "Submit for verification" is a controlled rolling-label AsyncButton wired to the real upload + submit, and the last step draws its check (stroke-dashoffset) and ends on the pulsing "Waiting for Skydrop to see it" pending StatusChip; withdrawal request Dialog, then a ConfirmDialog (busy confirm, locked while the request runs) restating amount, destination and consequence; toasts (u32) for "Top-up recorded…" and "Withdrawal requested."
- **/wallet/limits** — the automatic-withdrawal on/off is a u08 Switch (thumb springs, On/Off words in the track); hour is the u05 native Select (chevron turns on focus); balance to keep is a u33 TextField (floating label, icon chip); every change opens a ConfirmDialog restating the new schedule before the save; success/error toasts; "Your limits" rows are static info rows (info chip, no animation).
- **/freight** — KpiCard glow on four cards; the two counts ("Consignments billed", "Units charged") roll up once on first load (odometer), the two money cards take their `<Money>` as `figure`; Tabs liquid bead for the status filter; skeleton rows while loading; u27 empty state for no bills / no bills with that status; status as StatusChip (icon + word); the "How a bill is charged" note is a static info callout.

Reduced motion: every pattern above is handled by the global collapse in `brand/app.css`; the end states (filled stepper, drawn check, pending chip, final figures) are correct with no motion.
