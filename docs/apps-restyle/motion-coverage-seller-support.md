# Motion coverage — seller customers, tickets and needs attention

- **/customers** — KpiCard glow in each card's tone on four cards; the counts ("Customers", "Orders on this page", "Came back on this page", "Flagged on this page") roll up once on first load (odometer, landing on the same ungrouped string as before), and read "—" until answered; toolbar search pill with its focus glow; skeleton shimmer while the register loads; u27 empty state (neutral) for no customers / nobody matches, with a "Clear search" action; risk as a StatusChip (icon + word); rows tint on hover only (no per-row entrance); the Open / Hide control's chevron turns as the record opens into an inset well under its row; pagination with the travelling current-page pill. Inside the opened record: skeleton while it loads, "Correct details" opens a Dialog (scale + fade, bottom sheet under 640px) with u33 TextFields (floating labels, live E.164 error on the alternate phone), the u05 native Select (chevron turns on focus) and a TextArea with a 0/2000 counter; "Save corrections" is a rolling-label AsyncButton (Saving… → Saved, or Not saved with the server verdict above the fields) wired to the real PATCH; "Remove" opens a ConfirmDialog restating the phone number, the consequence and "Keep it" / "Remove customer" (busy confirm while the DELETE runs, stays open with the verdict on a refusal); u32 toasts for "Customer updated." and "Customer removed."; the address-history table shows a skeleton, u27 empty state or the data table (hover tint only).
- **/tickets** — KpiCard glow on three cards; the two counts roll up once on first load (odometer, same string as before), the refund card takes its `<Money>` as `figure`; Tabs liquid bead travelling between All tickets / Open / Reviewing / Closed; toolbar search pill; skeleton rows while loading; u27 empty state (with "Raise an issue" when allowed); status as StatusChip (icon + word); rows tint on hover (no per-row entrance) and the whole row opens the ticket; the courier "View / Hide" control's chevron turns as the row opens an inset well holding the u17 Timeline of updates (connector fills once, done steps carry checks) and the courier thread (skeleton, StatusChips, verbatim messages); "Raise an issue" opens a Dialog with the u05 Selects, a u33 TextField and a TextArea with the 0/300 counter, and "Raise issue" is a rolling-label AsyncButton (Raising… → Raised, or Not raised with the verdict) wired to the real POST; u32 toast "Issue raised…".
- **/tickets/[id]** — back link; KpiCard glow on four cards (status word, refund `<Money>`, dates and the order link handed over as `figure`, so no count-up); the refund card is a green-tinted card with a soft glow when money landed; StatusChip in the header and in the facts; skeleton blocks while the ticket loads; ErrorState with retry on a refusal. Conversation: skeleton while the thread loads; bubbles are static (no entrance) with the seller's on the right in the accent tint; the reply box is a TextArea with a floating label and a 0/2000 counter; **"Send" is the PaperPlaneSendButton** — busy while the real POST runs, and only after a real success the button folds into a paper plane that flies off (≤ 0.7s) and springs back green ("Sent"), usable again as soon as the plane is gone; a refusal shakes it red ("Not sent") with the server verdict in a toast and no plane.
- **/needs-attention** — KpiCard glow on two cards; counts roll up once when all three queries have answered, "—" before; each order and parcel is a ListRow with a severity stripe (high for "Waiting on your decision", medium for "Let go", critical / high / medium by nights out), an icon chip, a chevron that nudges on hover and a hover lift (no per-row entrance); the whole row opens the order; skeleton rows while loading; ErrorState with retry; when nothing needs the seller, the **positive u27 empty state** ("Nothing of yours needs you").

Reduced motion: every pattern above is handled by the global collapse in `brand/app.css`; end states (final counts, filled timeline, the reply button back in place with the "Sent" pill beside it) are correct with no motion. Nothing on these pages delays a navigation.
